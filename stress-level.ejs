<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stress Level Assessment</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <nav class="bg-blue-600 p-4 shadow-lg">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-white text-xl font-bold">Stress Level Assessment</h1>
      <div class="space-x-4">
        <a href="/" class="text-white font-medium hover:text-gray-200 transition-all duration-200">Home</a>
        <a href="/logout" class="text-white font-medium hover:text-gray-200 transition-all duration-200">Logout</a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mx-auto p-6">
    <h2 class="text-2xl font-semibold text-center mb-4">Stress Level Assessment</h2>
    <% if (user) { %>
    <form action="/stress-level" method="POST" class="max-w-lg mx-auto bg-white p-8 rounded-lg shadow-lg">
      <div class="mb-4">
        <label for="stressLevel" class="block text-gray-700 font-semibold">Rate your stress level (1-10)</label>
        <input type="number" id="stressLevel" name="stressLevel" class="mt-2 p-2 w-full border border-gray-300 rounded-lg" min="1" max="10" required>
      </div>
      <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600">Save</button>
    </form>
    <% } else { %>
    <p class="text-center text-red-500">Please log in to assess your stress level.</p>
    <% } %>
  </div>
</body>
</html>
