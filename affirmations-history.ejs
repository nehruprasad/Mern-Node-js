<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Affirmation History</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-6">
    <h2 class="text-2xl font-semibold text-center mb-4">Your Affirmation History</h2>
    <% if (affirmations.length > 0) { %>
    <div class="bg-white p-6 rounded-lg shadow-lg">
      <ul class="space-y-4">
        <% affirmations.forEach(affirmation => { %>
        <li class="border-b py-2">
          <p class="text-gray-700"><strong><%= affirmation.affirmation %></strong></p>
          <p class="text-gray-500 text-sm">Date: <%= affirmation.date %></p>
        </li>
        <% }) %>
      </ul>
    </div>
    <% } else { %>
    <p class="text-center text-gray-500">You have no recorded affirmations yet.</p>
    <% } %>
  </div>
</body>
</html>
