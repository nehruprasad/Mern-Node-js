<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stress History</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-6">
    <h2 class="text-2xl font-semibold text-center mb-4">Your Stress Level History</h2>
    <% if (stressLevels.length > 0) { %>
    <div class="bg-white p-6 rounded-lg shadow-lg">
      <ul class="space-y-4">
        <% stressLevels.forEach(stress => { %>
        <li class="border-b py-2">
          <p class="text-gray-700">Stress Level: <strong><%= stress.stress_level %></strong></p>
          <p class="text-gray-500 text-sm">Date: <%= stress.date %></p>
        </li>
        <% }) %>
      </ul>
    </div>
    <% } else { %>
    <p class="text-center text-gray-500">You have no recorded stress levels yet.</p>
    <% } %>
  </div>
</body>
</html>
